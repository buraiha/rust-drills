# Day08: struct/impl/method

**対象crate:** `day08_structs`

このドリルは「**動かす → 読む → 改造 → 壊して直す → テスト → コミット**」の反復で身につけます。

## タイムボックス（60分）

- 0〜5分：ウォームアップ（fmt/clippy/test）
- 5〜15分：読む（今日のポイントを説明できるようにする）
- 15〜45分：改造ドリル（1〜2個）
- 45〜55分：壊して直すドリル（1個）
- 55〜60分：メモ＆コミット

## 実行コマンド

```bash
./rust.sh cargo test -p day08_structs
./rust.sh cargo run  -p day08_structs
# 仕上げ
./rust.sh cargo fmt
./rust.sh cargo clippy -p day08_structs -- -D warnings
```

## 0) ウォームアップ（チェック）

- [ ] `cargo test` が通る
- [ ] `cargo run` が動く
- [ ] `clippy` が通る（warning 0）

## 1) 読むドリル（10分）

次の問いに **自分の言葉で**答えられればOK（メモは3行で十分）。

- [ ] このcrateの「入口」はどこ？（`main.rs` の何をしてる？）
- [ ] `lib.rs` の責務は？（関数/型が何を隠蔽してる？）
- [ ] 今日の“Rustっぽいポイント”はどこ？（型/所有権/Result/trait など）

## 2) 改造ドリル（必須：1つ以上）

以下から **1つ以上**選んで実装。

- [ ] **改造1**: `birthday()` で年齢+1
- [ ] **改造2**: `Display` 実装（標準出力を整える）

**コツ:** 改造は小さく。動く状態に戻してから次。

## 3) 壊して直すドリル（必須：1つ）

わざと壊して、**コンパイラ/テストのエラーを読んで**戻す。

- [ ] **破壊1**: `age` を `u8`→`String` にして壊して戻す（型の恩恵確認）

## 4) テストを1本増やす（推奨）

- [ ] 境界値（空/0/負数/大きい値/Unicodeなど）を1つ追加
- [ ] 「改造した箇所」をテストで固定

## 合格条件（Definition of Done）

- [ ] 改造を1つ入れて動く
- [ ] 壊して直すを1回やった
- [ ] fmt/clippy/test が通る

## メモ（1分）

- 学んだこと（3行）
- ハマったこと（1行）
- 明日やること（1行）

## コミット例

```bash
git add .
git commit -m "Day08: struct/impl/method"
```

